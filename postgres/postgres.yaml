db:
  image: postgres:9.6.3-alpine
  environment:
    POSTGRES_PASSWORD: docker
    POSTGRES_USER: docker
#  volumes:
#     - ./createDB.sql:/docker-entrypoint-initdb.d/
#     - ./createTables.sql:/docker-entrypoint-initdb.d/
